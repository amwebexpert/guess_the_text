name: orientation_aware_widgets
description: Device orientation aware widgets collection that combines existing Flutter compoments to simplify UI development.
version: 1.0.3
homepage: https://github.com/amwebexpert/guess_the_text/tree/master/orientation_aware_widgets

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
